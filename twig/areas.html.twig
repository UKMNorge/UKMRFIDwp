{% extends "_layout.html.twig" %}

{% block header %}
	<h1>
		<img src="/wp-content/plugins/UKMRFIDwp/img/id-48.png" width="40" style="float: left; margin-right: 10px" />
		<a href="?page={{ GET('page') }}">RFID</a> / 
		Områder
	</h1>

	<p class="">
		<a href="?page={{ GET('page') }}&action=area_add" class="btn btn-mini btn-xs btn-success">Legg til nytt område</a>
	</p>
{% endblock %}

{% block content %}
	<table class="table table-striped">
		<thead>
			<tr>
				<th>Område</th>
				<th>Personer</th>
				<th>Scannere</th>
			</tr>
		</thead>
		
		<tbody>
			{% for area in areas %}
				<tr>
					<td>{{ area.getName() }}</td>
					<td>{{ area.getCount() }}</td>
					<td>{{ area.getScanners()|length }}</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="3">Ingen områder aktive</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endblock %}